# CONTRIBUTING

## Branching

- Base branch for active work: `develop`
- Protected release branches: `main`, `develop`

## Commit Format

Use `type(scope): summary`.

## Verification

Run:

- `npm run lint`
- `npm run typecheck`
- `npm run test`
